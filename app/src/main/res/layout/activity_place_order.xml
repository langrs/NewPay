<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    android:id="@+id/activity_single_frame_drawer"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:fitsSystemWindows="true"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        android:orientation="vertical">

        <include
            layout="@layout/toolbar" />

        <FrameLayout
            android:id="@+id/activity_single_frame"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </LinearLayout>

    <android.support.design.widget.NavigationView
        android:id="@+id/activity_single_frame_drawer_content"
        android:layout_width="@dimen/drawer_width"
        android:layout_height="fill_parent"
        android:layout_gravity="left"
        android:background="@color/White"
        android:fitsSystemWindows="true"
        app:insetForeground="#44000000"
        app:itemBackground="@drawable/bg_menu_item_selector"
        app:itemTextColor="@color/menu_item_color_selector"
        app:menu="@menu/home_menu"
        />
        <!--app:headerLayout="@layout/drawer_content_header"-->

</android.support.v4.widget.DrawerLayout>